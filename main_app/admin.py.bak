from django.contrib import admin
from .models import StaffPosition, Staff, Streamer  # <-- Vérifier ces imports

@admin.register(StaffPosition)
class StaffPositionAdmin(admin.ModelAdmin):
    list_display = ('name',)
    search_fields = ('name',)

@admin.register(Staff)
class StaffAdmin(admin.ModelAdmin):
    list_display = ('username', 'first_name', 'last_name', 'email', 'position')

@admin.register(Streamer)
class StreamerAdmin(admin.ModelAdmin):
    list_display = ('twitch_name', 'email', 'created_at', 'is_present')